<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
      <li>
    <%=link_to user.name,"#{user.id}"%>

   |<%=link_to "delete","#{user.id}",method: :delete, date:{confirm:"You sure?"} %>

      </li>
  <%end%>
</ul>